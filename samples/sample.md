# Lex sample document

A paragraph with **bold**, *italic*, `inline code` and a link to
<https://example.com> that should autolink.

This line ends with a hard break  
and this is the line after it.

## A fenced code block

```kotlin
fun main() {
    // Trailing whitespace on the next line is content, not noise.   
    println("hello")
}
```

## A table

| Column | Type | Notes |
| --- | --- | --- |
| `id` | Int | primary key |
| `name` | String | may be blank |

> A blockquote, to check the preview stylesheet.

- list item one
- list item two
  - nested item
